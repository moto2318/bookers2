<%= render 'post_image/user', user: current_user, book: Book.new %>
<!--ユーザー一覧（自分含めた各ユーザー画像・名前・詳細画面へのリンク）-->
<h4>Users</h4>
  <h2>image</h2>
  <h2>name</h2>
  
<% @users.each do |user| %>
  <div>
    <p>投稿ユーザー画像：<%= image_tag user.get_profile_image(100, 100) %></p>
    <p>ユーザーネーム：<%= user.name %></p>
    <p>詳細へのリンク：<%= link_to "Show", user_path(user) %></p>
  </div>
<% end %>